# design-system-skill — v1.8.0 snapshot

**Frozen**: 2026-03-27
**Changes from v1.7.0**: Adaptive MCP tier detection system — `init.md` probe cascade, operation dispatch table, OFFICIAL-tier JS equivalents. 9 production-validated fixes applied to SKILL.md. Protocol 9 Strangler Fig enhanced with M0 page selection, M3.5 semantic contextualization, M4 lockPrimitiveScopes. figma-ds-modernization.md bundled.

See changelog.md for full diff.

## Frontmatter at this version

- `version: 1.8.0`
- `disable-model-invocation: false`
- `allowed-tools: Read, WebSearch, TaskCreate, TaskUpdate, mcp__figma-canvas__figma_execute, mcp__figma-canvas__figma_take_screenshot, mcp__figma-canvas__figma_get_status, mcp__figma-canvas__figma_get_selection, mcp__figma-canvas__figma_get_variables, mcp__figma-canvas__figma_batch_create_variables, mcp__figma-canvas__figma_batch_update_variables, mcp__figma-canvas__figma_setup_design_tokens, mcp__figma-canvas__figma_get_styles, mcp__figma-canvas__figma_get_file_data, mcp__figma-canvas__figma_capture_screenshot, mcp__figma-canvas__figma_get_console_logs, mcp__figma-canvas__figma_get_component, mcp__figma-canvas__figma_search_components, mcp__figma-console__figma_execute, mcp__figma-console__figma_take_screenshot, mcp__figma-console__figma_get_status, mcp__claude_ai_Figma__use_figma, mcp__claude_ai_Figma__generate_figma_design, mcp__claude_ai_Figma__get_design_context, mcp__claude_ai_Figma__get_screenshot, mcp__claude_ai_Figma__get_metadata, mcp__claude_ai_Figma__get_variable_defs, mcp__claude_ai_Figma__get_code_connect_suggestions, mcp__claude_ai_Figma__add_code_connect_map, mcp__claude_ai_Figma__send_code_connect_mappings`
- 9 `<example>` blocks
- 6 reference files
- 17 protocols total (Protocols 1–16 inline, Protocol 17 in figma-ds-modernization.md)

## New at this version

| Area | Change |
|------|--------|
| `init.md` | New file — Phase 0a MCP tier detection probe cascade: `CANVAS_FULL` → `OFFICIAL` → `REST_ONLY` → `NONE`. Full operation dispatch table. 5 OFFICIAL-tier JS equivalents. `phase0b_official_stub()` for timeout fallback. |
| SKILL.md Phase 0a | Rewritten — reads `init.md`, sets `ACTIVE_TIER`, adapts all subsequent protocols accordingly. Graceful degradation replaces hard stop. |
| Protocol 9 M0 | New phase: page selection scan before any migration work begins. Reports page names + node counts. |
| Protocol 9 M1 | Enhanced: nodeTypes breakdown in hardcoded inventory output. |
| Protocol 9 M2 | `resolveAlias()` required for semantic token color-matching; semantic-first mapping preference. |
| Protocol 9 M3 | Replaced with batched `SKIP_TYPES = new Set(['COMPONENT_SET', 'COMPONENT'])` pattern. Prevents white/black boxes on component grids. |
| Protocol 9 M3.5 | New phase: semantic contextualization — maps remaining neutral fills to `text/disabled` vs `text/tertiary` based on node context. |
| Protocol 9 M4 | Step 6 added: `lockPrimitiveScopes()` — sets `scopes = []` on all `Color/Primitives` variables after migration completes. |
| Protocol 8 Tier 4 | `unscopedPrimitives = 0` now required for Tier 4 — Mature classification. |
| Shared Helpers | `resolveAlias(varId)` added — walks `VARIABLE_ALIAS` chains to concrete `{r,g,b,a}` values. |
| figma-ds-modernization.md | Bundled in repo (was only in `~/.claude/skills/`). Includes Protocols 8–11, `resolveAlias()`, `colorsMatch()`. Version bumped to 1.2.0. |
| research-layout-composition.md | Bundled in repo. |

## Production-validated fixes at this version

1. `loadAllPagesAsync` removed from Phase 0b — not supported at OFFICIAL tier; `figma.root.children` used instead
2. `phase0b_official()` renamed to `phase0b_official_stub()` with fallback-only notice; full script runs at OFFICIAL tier
3. COMPONENT_SET/COMPONENT contamination guard added to Protocol 9 M3
4. `resolveAlias()` added to walk alias chains in M2 color-matching
5. `SKIP_TYPES` batched bind pattern replaces naive `findAll()` loop
6. `lockPrimitiveScopes()` added as M4 final step
7. `unscopedPrimitives` metric added to Phase 0b health output
8. Font loading pre-check rule added to Protocol 7 (`listAvailableFontsAsync()` mandatory before `setTextStyleIdAsync`)
9. Phase 0b reading questions updated to include `unscopedPrimitives` and `resolvedVariableModes`
